# 设计取舍（简述）

系统强调 **可操作性**与**端到端可追溯**：单机 agent 捕获开发者工具轨迹，服务端做聚合与洞察。**单体 Spring Boot** 降低早期迭代成本；在性能或团队规模需要时再模块化。

UI 以内嵌静态资源分发，简化部署。**工程向文档**（分层、安全、黄金原则等）在 `docs/` 根下各子目录；**产品/方案向设计稿**统一放在 **`docs/design/`**（`legacy/` 为早期 v1.x 存档）。
