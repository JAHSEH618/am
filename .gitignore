# 本地环境变量 / 密钥（切勿提交）—— 保留 .env.example 作为模板
.env
.env.local

# 构建产物
agent/dist/
server/build/
server/.gradle/
server/src/main/resources/static/
server/bin/

# IDE 本地工程元数据（Eclipse / Buildship，即 VS Code Java 扩展导入 Gradle 时生成）
server/.classpath
server/.factorypath
server/.project
server/.settings/

# 工具本地缓存 / 中间产物（非源码，不入库）
**/.impeccable/
docs/superpowers/

# 系统文件
.DS_Store

# AI agent skills（skills CLI 安装产物 / Claude Code 本地链接，非产品代码，本地可用即可）
.agents/
skills-lock.json
.claude/skills/
