# Core Beliefs（不可轻易违背）

1. **Agent 可信度**：上报链路必须有可验证身份与防重放/滥用机制（持续演进）。
2. **单一后端进程**：当前以 Spring Boot 单体交付，避免为了「纯 DDD」拆模块而增加运维面。
3. **UI 与 API 同步版本**：生产静态资源由 Gradle 构建嵌入，避免前后端版本漂移。
4. **机器可读约束**：架构边界用测试强制执行，不靠口头约定。