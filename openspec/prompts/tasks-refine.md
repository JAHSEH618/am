# tasks 细化 Prompt

适用方式：普通 Prompt，不对应 OpenSpec 原生命令  
使用口径：在 Cursor 对话中直接输入，或由团队自定义 Skill 承接

```text
请基于已生成的 tasks、spec 和 design，对当前 tasks 做审核和细化。

要求：
1. 检查每个 Task 是否边界清晰、可独立执行、可独立验证
2. 补齐缺失的禁止改动项、完成标准、验证方式
3. 明确阶段顺序、阶段依赖和阶段交付标准
4. 对中高风险任务，体现分阶段实施和分阶段验证思路
```
